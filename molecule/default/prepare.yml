---
- name: Prepare
  hosts: all
  become: true

  tasks:
    - name: update apt cache
      apt:
        update_cache: true
        cache_valid_time: 600
      when: ansible_os_family == 'Debian'
      changed_when: false
